job_name: sgn-docker-harden-v5
jobs_dir: jobs
agents:
  - name: aider
    model_name: openai/qwen2.5-coder:14b
    kwargs:
      AIDER_MODEL: "openai/qwen2.5-coder:14b"
      AIDER_OPENAI_API_BASE: "http://host.docker.internal:11434/v1"
environment:
  type: docker
  kwargs:
    extra_hosts:
      host.docker.internal: host-gateway
tasks:
  - name: sgn-docker-prod-harden
    path: harbor-tasks/sgn-docker-prod-harden
    # Here we can specify how to mount the repo
