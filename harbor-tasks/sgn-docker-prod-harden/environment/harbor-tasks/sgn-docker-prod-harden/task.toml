version = "1.0"

[metadata]
description = "Hardening do Docker de produção do SGN com healthcheck, limites e backup"
tags = ["sgn", "docker", "production", "security", "devops"]

[verifier]
timeout_sec = 600.0

[agent]
timeout_sec = 1200.0

[environment]
build_timeout_sec = 600.0
cpus = 2
memory_mb = 4096
storage_mb = 10240
gpus = 0
allow_internet = true
mcp_servers = []

[agent.env]
OPENAI_API_BASE = "http://host.docker.internal:11434/v1"
OPENAI_API_KEY = "ollama"
AIDER_MODEL = "openai/qwen2.5-coder:14b"
AIDER_OPENAI_API_BASE = "http://host.docker.internal:11434/v1"

[verifier.env]

[solution.env]
