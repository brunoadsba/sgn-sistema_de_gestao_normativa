import { PageLoading } from '@/components/loading/LoadingSpinner'

// 🚀 LOADING GLOBAL OTIMIZADO - Next.js 15
export default function Loading() {
  return (
    <PageLoading 
      title="Carregando SGN..."
      description="Preparando interface empresarial"
    />
  )
}
